---
description: Deploy to Vercel (powershot.org)
subtask: true
---

Deploy this project to Vercel for the powershot.org domain.

Steps:
1. Run `vercel --prod --yes` to deploy to production.
2. After deployment completes, verify the deployment succeeded by checking the output URL.
3. If the deployment fails, read the error output carefully and fix the issue before retrying.

Do not skip the build step — let Vercel handle the build. Just run the deploy command and report the result.